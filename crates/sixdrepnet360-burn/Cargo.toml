[package]
name = "sixdrepnet360-burn"
version = "0.1.0"
edition = "2024"

[features]
pretrained = ["dirs", "burn/network"]

[dependencies]
burn = { workspace = true, features = ["train", "vision"] }
burn-store.workspace = true
dirs = { workspace = true, optional = true }
